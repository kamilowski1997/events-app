export type Event = {
  firstName: string;
  lastName: string;
  email: string;
  eventDate: Date;
};

export type EventDto = {
  firstName: string;
  lastName: string;
  email: string;
  eventDate: string;
};
